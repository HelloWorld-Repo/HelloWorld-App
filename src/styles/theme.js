import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primary: '#00DFC0',
      secondary: '#202020',
      error: '#ff5b5b',
      background: '#FFFFFF'
    },
};

export default theme;